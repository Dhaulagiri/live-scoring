<div class="navbar navbar-default">
  <div class="navbar-header">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-responsive-collapse">
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    {{#link-to 'index'classNames="navbar-brand"}}Comp Scoring{{/link-to}}
  </div>
  <div class="navbar-collapse collapse navbar-responsive-collapse">
    <ul class="nav navbar-nav">
        {{#link-to 'climbers' tagName="li" href=flase}}<a href="#">Climbers</a>{{/link-to}}
        {{#link-to 'comps' tagName="li" href=flase}}<a href="#">Comps</a>{{/link-to}}
    </ul>
    <ul class="nav navbar-nav navbar-right">
      {{#if session.isAuthenticated}}
          <li>
            <a href="#" {{action invalidateSession}}>Logout</a>
      {{else}}
          {{#link-to 'login' tagName="li" href=false}}<a href="#">Login</a>{{/link-to}}
      {{/if}}
    </ul>
  </div>
</div>
